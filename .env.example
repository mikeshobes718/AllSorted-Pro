# Copy to .env.local for local dev. Production: set in Vercel → Project → Settings → Environment Variables.

SCRAPER_SECRET=

# Quo (OpenPhone) — server-side only; never expose to the browser
QUO_API_KEY=
QUO_DEFAULT_PHONE_E164=+19177733276
